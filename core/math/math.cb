function sqrt(number: float): float {
    if (number == 0.0) return 0.0
    if (number < 0.0) return 0.0

    mut root := number / 3.0
    for (i := 0; i < 32; i = i + 1) {
        root = (root + number / root) / 2.0
    }
    return root
}